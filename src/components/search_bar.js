/**
 * 이 컴포넌트의 목적
 * 
 * 상단의 입력창, 타이핑 가능
 * 검색어 단어를 입력하면, 오른쪽에 비디오 리스트 업데이트가 가능
 */
import React from 'react'; //jsx를 가지면 모든 컴포넌트에 import를 해줘야 한다.

const SearchBar = () => {
  return <input type="text" />; //React.createElement("input", null);
};

export default SearchBar;